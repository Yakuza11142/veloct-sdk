module UI.WorldMap;

struct MapMarker {
    world_pos: vec3,
    marker_type: u8,
    is_visible: bool
}

public fn project_to_minimap(world_pos: vec3, player_pos: vec3) -> vec2 {
    let delta = world_pos - player_pos;
    return vec2(delta.x * 0.1 + 100.0, delta.z * 0.1 + 100.0);
}
