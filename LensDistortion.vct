module Tracking.Lens;

public fn calculate_uv_distortion(uv: vec2, k1: f32, k2: f32) -> vec2 {
    let r2 = (uv.x - 0.5) * (uv.x - 0.5) + (uv.y - 0.5) * (uv.y - 0.5);
    let factor = 1.0 + k1 * r2 + k2 * r2 * r2;
    return vec2(0.5 + (uv.x - 0.5) * factor, 0.5 + (uv.y - 0.5) * factor);
}
