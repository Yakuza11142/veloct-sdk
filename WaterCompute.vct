module VFX.Water;

[GPUCompute]
public kernel fn compute_water_surface(mut mesh_vertices: [vec3], time: f32) {
    let idx = dispatch_thread_id_x();
    let v = mesh_vertices[idx];
    mesh_vertices[idx].y = math_sin(v.x * 0.5 + time) * 0.8 + math_cos(v.z * 0.5 + time) * 0.8;
}
