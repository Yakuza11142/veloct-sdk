module Lighting.Shadows;

public fn compute_shadow_alpha(light_space_pos: vec3, depth_map_val: f32) -> f32 {
    if light_space_pos.z > depth_map_val + 0.005 {
        return 0.65; // Shadow density mask
    }
    return 0.0;
}
